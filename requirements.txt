# jpzh-subtitle 运行时依赖
# 安装: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#
# 注: ctranslate2 自带 CUDA 运行支持, 但需要 cublas64_12.dll / cudart64_12.dll。
# 这些 DLL 已包含在 models/llamacpp/ 目录中（llama.cpp 预编译二进制附带）。
# config.py 会自动将该目录加入 PATH。

faster-whisper>=1.0.0
ctranslate2>=4.0.0
requests>=2.28.0
